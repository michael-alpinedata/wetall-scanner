# REmarques : 
## BUGS:
- depuis un mobile, la bascule de wetall vers decathlon ne fonctionne pas "une erreur est survenue lors de la récupération du produit" si l'appli décathlon est installée sur le mobile. Si l'appli n'est pas installée, ça fonctionne (peut etre?).


curl -X POST https://wetall-links-checker.onrender.com \
     -H "x-secret-key: "